export function data(message) {
    return message.data;
}

export function origin(message) {
    return message.origin;
}

export function source(message) {
    return message.source;
}

export function client_id(client) {
    return client.id;
}