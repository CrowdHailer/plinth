export function startContainer(range) {
  return range.startContainer;
}

export function startOffset(range) {
  return range.startOffset;
}

export function endContainer(range) {
  return range.endContainer;
}

export function endOffset(range) {
  return range.endOffset;
}
